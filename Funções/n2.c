/*Crie uma função chamada encontrarMaior que receba um vetor de inteiros e seu
tamanho como parâmetros e retorne o maior valor encontrado no vetor.*/

#include <stdio.h>
#define TAM 5

// função de entrada e saida dos valores
int main()
{
    // retorne ao valor maior encontrado pelo vetor
    int encontrar_maior(int vetor[TAM], int tamanho)
    {
        int maior = 0;
        for (int i = 0; i < TAM; i++)
        {
            encontrar_maior = maior(2);
        }

        return maior;
    }
    printf("O maior: %d", maior);

    return 0;
}
