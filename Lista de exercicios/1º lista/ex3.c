// Crie um programa que imprima a media artmetica entre os numeros 8,9 e 7
#include <stdio.h>

int main(){

    float num1 = 8, num2 = 9, num3 = 7, soma;

    soma = (num1 + num2 + num3) / 3;
    
    printf("Resultado = %.1f \n", soma);

    return 0;
}

// Feita com sucesso.